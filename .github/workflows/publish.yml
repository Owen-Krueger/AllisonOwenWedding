name: Deploy to Docker Hub

on:
  push:
    branches:
    - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Check out repo
        uses: actions/checkout@v2

      - name: Login to Docker Hub
        uses: docker/login-action@v1
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_ACCESS_TOKEN }}

      - name: Set up Docker Buildx
        id: buildx
        uses: docker/setup-buildx-action@v1

      # Server
      - name: Build and push Server
        id: docker_build_server
        uses: docker/build-push-action@v2
        with:
          context: ./src/Server
          push: true
          tags: ${{ secrets.DOCKERHUB_USERNAME }}/allison-owen-wedding-server:latest

      - name: Image digest (Server)
        run: echo ${{ steps.docker_build_server.outputs.digest }}

      # Client      
      - name: Build and push Client
        id: docker_build_client
        uses: docker/build-push-action@v2
        with:
          context: ./src/Client
          push: true
          tags: ${{ secrets.DOCKERHUB_USERNAME }}/allison-owen-wedding-client:latest

      - name: Image digest (Client)
        run: echo ${{ steps.docker_build_client.outputs.digest }}