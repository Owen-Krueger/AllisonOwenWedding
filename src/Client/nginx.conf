events {}
	http {
		include mime.types;
		types {
			application/wasm wasm;
		}
		server {
			listen 80;
			listen [::]80;
			index index.html;
			server_name allisonandowen.love www.allisonandowen.love
			location / {
				root /var/www/web;
				try_files $uri $uri/ /index.html =404;
			}
		}
	}